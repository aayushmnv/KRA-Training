"use strict";
// An interface is like a contract for objects — it defines the shape (structure)
//  that an object must follow.
// create a User type object 
const aayush = {
    name: "Aayush",
    age: 23,
    isAdmin: true,
};
const u1 = { name: "Pandey" }; // valid
const config = {
    version: "1.0.0",
    darkMode: true,
};
const e1 = {
    name: "Aayush",
    employeeId: 101,
};
